# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,45.10.41.250

# Database Configuration
DB_NAME=okurmen_db
DB_USER=okurmen_user
DB_PASSWORD=okurmen_password_change_in_production
DB_HOST=db
DB_PORT=5432

# CORS Configuration
# Comma-separated list of allowed origins (e.g., http://localhost:3000,https://example.com)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080
# Allow credentials (cookies, authorization headers) - True or False
CORS_ALLOW_CREDENTIALS=True
# For development only: allow all origins - True or False (NOT recommended for production)
CORS_ALLOW_ALL_ORIGINS=False

